import React from "react";
import ChatViewer from "../components/ChatViewer.js";

const Content = () => {
  // const myarray = [...Array(50).keys()];

  return (
    <div className="container">
      <ChatViewer />
    </div>
  );
};

export default Content;
